# Group 3 rollback

Until a workload opts into RandDurableRuntime, operational behavior is unchanged. Rollback is therefore removal/revert of the Group 3 contract. Future workload migrations must be separately feature-gated so disabling an adapter returns ownership to the prior governed execution path without data loss.
