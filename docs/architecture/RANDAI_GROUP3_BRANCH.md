# Group 3 branch

Branch: `randai/group3-durable-runtime` based directly on the Group 2 main commit. It is intentionally behind no main commits at PR creation time. Squash merge is preferred after green CI to collapse implementation commits into one auditable release commit.
