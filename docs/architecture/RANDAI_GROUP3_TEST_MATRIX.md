# Group 3 test matrix

The `test/randai-group3-*.test.js` gate covers authorization, cross-hotel denial, denied start, idempotency, terminal stability, checkpoint continuity, resume reauthorization, actor substitution denial, knowledge refresh order, bounded retry, non-retryable failure, workflow version mismatch, cancellation, store contract, package-lock/dependency ownership, public exports, existing recovery coexistence and external executor boundary.
