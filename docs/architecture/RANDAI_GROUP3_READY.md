# Group 3 ready for CI

Implementation is complete on the feature branch. Merge is permitted only after the dedicated Group 3 workflow and existing repository CI are green. If CI exposes a regression, fix the branch before merge rather than bypassing the gate.
