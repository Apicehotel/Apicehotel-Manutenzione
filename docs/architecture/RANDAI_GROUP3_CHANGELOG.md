# Group 3 changelog

Adds a governed durable workflow boundary without changing existing production schedules or installing external orchestration packages. Existing Group 1/2 boundaries and recovery contracts are prerequisites in CI. The branch is intentionally safe to merge without activating new external infrastructure.
