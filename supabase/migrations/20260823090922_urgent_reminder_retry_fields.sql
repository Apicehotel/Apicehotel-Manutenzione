alter table public.urgent_reminder_jobs add column if not exists next_attempt_at timestamptz; update public.urgent_reminder_jobs set next_attempt_at=scheduled_at where next_attempt_at is null; alter table public.urgent_reminder_jobs alter column next_attempt_at set not null; create index if not exists urgent_reminder_jobs_next_idx on public.urgent_reminder_jobs(status,next_attempt_at);
