Follow the repository root `AGENTS.md` as the canonical engineering contract for RandApp. Read `README.md` and `FRONTEND_ARCHITECTURE.md` for current context. Preserve multi-hotel isolation, Supabase/RLS authorization, iOS+Android+Windows support, PWA cache safety, notification priority policy, and the repository quality gates. Do not treat navigation as authorization and do not modify already-applied migrations; add a later migration instead.
