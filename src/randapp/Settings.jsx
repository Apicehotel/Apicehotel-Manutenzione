import { useEffect, useMemo, useState } from 'react'
import { HOTELS, ROLES, ROLE_PERMISSIONS } from '../config.js'
import { supabase } from '../supabase.js'
import { supabaseUrl } from '../supabase.js'
import { fetchUsers, insertUser, updateUserRow, updateUserPin, setUserActive, permanentlyDeleteUser, getTechnicianLink } from '../users-data.js'
import { fetchAllSensors, updateSensorVisibility, syncSensorsFromEwelink } from '../sensors-admin-data.js'
import { Button, Card, Field, TextInput, Icon, Badge, Spinner, EmptyState, Modal, ConfirmDialog, UiSizeControl, ThemeControl } from './ui.jsx'

const PERMISSION_LABELS = {
  manage_users: 'Gestione utenti', manage_all_hotels: 'Tutte le strutture', create: 'Crea segnalazioni', assign: 'Assegna lavori',
  complete: 'Completa lavori', read_all_departments: 'Tutti i reparti', planning_sale: 'Planning Sale', take_charge: 'Presa in carico', read_own_hotel: 'Lettura struttura',
}
const NAV_ITEMS = [
  ['home', 'Home'], ['issues', 'Segnalazioni'], ['interventions', 'Interventi'], ['planning_work', 'Planning'],
  ['housekeeping', 'Housekeeping'], ['temperature', 'Temperature'], ['urgent', 'Avvisi urgenti'],
  ['technicians', 'Rubrica tecnici'], ['structure', 'Cambia struttura'], ['profile', 'Il mio profilo'], ['feedback', 'Feedback'],
]
const PLACEMENTS = [['bottom', 'Sotto'], ['side', 'Laterale'], ['off', 'Off']]
const NAV_KEY = 'role_navigation_v1'
const ALL_HOTEL_IDS = HOTELS.map((h) => h.id)

function UsersTab() {
  const [users, setUsers] = useState([]), [loading, setLoading] = useState(true), [search, setSearch] = useState(''), [creating, setCreating] = useState(false), [message, setMessage] = useState(''), [techLink, setTechLink] = useState(null), [confirmDel, setConfirmDel] = useState(null), [openGroups, setOpenGroups] = useState({})
  const empty = { name: '', role: 'Reception', email: '', phone: '', pin: '', hotels: [...ALL_HOTEL_IDS] }
  const [draft, setDraft] = useState(empty)
  const reload = () => fetchUsers(ALL_HOTEL_IDS).then((r) => setUsers(r.users || [])).catch(() => setUsers([])).finally(() => setLoading(false))
  useEffect(() => { reload() }, [])
  const filtered = useMemo(() => { const q = search.trim().toLowerCase(); return q ? users.filter((u) => `${u.name} ${u.role} ${u.email || ''}`.toLowerCase().includes(q)) : users }, [users, search])
  const groups = useMemo(() => { const hk = new Set(['Governante','Capo Governante']); const hu = filtered.filter(u=>hk.has(u.role)); const byHotel=HOTELS.map(h=>({role:`Housekeeping · ${h.short}`,list:hu.filter(u=>(u.hotels||[]).includes(h.id))})).filter(g=>g.list.length); const known=ROLES.filter(r=>!hk.has(r)).map(r=>({role:r,list:filtered.filter(u=>u.role===r)})).filter(g=>g.list.length); const other=filtered.filter(u=>!ROLES.includes(u.role)); return [...byHotel,...known,...(other.length?[{role:'Altro',list:other}]:[])] },[filtered])
  const save=async(t,c)=>{if(t.protected)return setMessage('Account protetto: non modificabile');try{await updateUserRow(t.auth_user_id||t.id,c);await reload();setMessage('Modifiche salvate')}catch(e){setMessage(e?.message||'Errore durante il salvataggio')}}
  const toggleHotel=(t,id)=>{const hotels=(t.hotels||[]).includes(id)?t.hotels.filter(x=>x!==id):[...(t.hotels||[]),id];if(!hotels.length)return setMessage('Ogni utente deve mantenere almeno una struttura');save(t,{hotels})}
  const resetPin=async t=>{const pin=window.prompt(`Nuovo PIN di 4 cifre per ${t.name}`)||'';if(!/^\d{4}$/.test(pin))return setMessage('PIN non valido');try{await updateUserPin(t.auth_user_id||t.id,pin);setMessage(`PIN di ${t.name} aggiornato`)}catch(e){setMessage(e?.message||'Errore durante il cambio PIN')}}
  const toggleActive=async t=>{try{await setUserActive(t.auth_user_id||t.id,!t.active);await reload()}catch(e){setMessage(e?.message||'Errore')}}
  const remove=async t=>{try{await permanentlyDeleteUser(t.auth_user_id||t.id);await reload();setMessage(`${t.name} eliminato`)}catch(e){setMessage(e?.message||"Errore durante l'eliminazione")}finally{setConfirmDel(null)}}
  const showLink=async t=>{try{const token=await getTechnicianLink(t.auth_user_id||t.id);setTechLink({name:t.name,url:`${window.location.origin}/tecnico/${token}`})}catch(e){setMessage(e?.message||'Errore link')}}
  const create=async e=>{e.preventDefault();if(!draft.name.trim()||!/^\d{4}$/.test(draft.pin)||!draft.hotels.length)return setMessage('Inserisci nome, PIN di 4 cifre e almeno una struttura');try{await insertUser({...draft,name:draft.name.trim()});await reload();setDraft(empty);setCreating(false);setMessage(`${draft.name.trim()} aggiunto`)}catch(err){setMessage(err?.message||'Errore durante la creazione')}}
  if(loading)return <Spinner label="Carico gli utenti…" />
  return <section data-testid="settings-users"><div className="rs-page-title"><div><h1>Utenti</h1><p>Gestisci utenti, ruoli e accessi alle strutture</p></div><Button variant={creating?'ghost':'primary'} icon={creating?'close':'plus'} onClick={()=>setCreating(!creating)}>{creating?'Annulla':'Nuovo'}</Button></div>{creating&&<Card className="rs-card--pad" style={{marginBottom:14}}><form className="rs-form" onSubmit={create}><Field label="Nome"><TextInput value={draft.name} onChange={e=>setDraft({...draft,name:e.target.value})}/></Field><Field label="Ruolo"><select className="rs-select" value={draft.role} onChange={e=>setDraft({...draft,role:e.target.value})}>{ROLES.map(r=><option key={r}>{r}</option>)}</select></Field><Field label="PIN di 4 cifre"><TextInput icon="lock" inputMode="numeric" value={draft.pin} onChange={e=>setDraft({...draft,pin:e.target.value.replace(/\D/g,'').slice(0,4)})}/></Field><div className="rs-hotel-toggles">{HOTELS.map(h=><button type="button" key={h.id} className={`rs-hotel-toggle ${draft.hotels.includes(h.id)?'on':''}`} onClick={()=>setDraft({...draft,hotels:draft.hotels.includes(h.id)?draft.hotels.filter(x=>x!==h.id):[...draft.hotels,h.id]})}>{h.short}</button>)}</div><Button variant="primary">Salva utente</Button></form></Card>}<TextInput icon="search" value={search} placeholder="Cerca utente…" onChange={e=>setSearch(e.target.value)}/>{message&&<p className="rs-badge rs-badge--accent" style={{display:'inline-flex',margin:'12px 0'}}>{message}</p>}<div style={{marginTop:14}}>{groups.length===0?<EmptyState icon="users" title="Nessun utente"/>:groups.map(({role,list})=><div className="rs-role-group" key={role}><button className={`rs-role-group__head ${openGroups[role]===false?'':'open'}`} onClick={()=>setOpenGroups(c=>({...c,[role]:c[role]===false?true:false}))}><b>{role}</b><span>{list.length}</span><i><Icon name="chevronDown"/></i></button>{openGroups[role]!==false&&list.map(u=><Card key={`${role}-${u.auth_user_id||u.id}`} className="rs-usercard"><div className="rs-usercard__top"><div><strong>{u.name}</strong><small>{[u.email,u.phone].filter(Boolean).join(' · ')||u.department||'—'}</small></div>{u.protected&&<Badge tone="accent">Protetto</Badge>}</div><select className="rs-select" value={u.role} disabled={u.protected} onChange={e=>save(u,{role:e.target.value})}>{ROLES.map(r=><option key={r}>{r}</option>)}</select><div className="rs-hotel-toggles">{HOTELS.map(h=><button key={h.id} className={`rs-hotel-toggle ${(u.hotels||[]).includes(h.id)?'on':''}`} disabled={u.protected} onClick={()=>toggleHotel(u,h.id)}>{h.short}</button>)}</div><div className="rs-usercard__actions"><Button variant="ghost" size="sm" onClick={()=>resetPin(u)}>PIN</Button>{u.role==='Tecnico esterno'&&<Button variant="ghost" size="sm" onClick={()=>showLink(u)}>Link</Button>}<Button variant="ghost" size="sm" onClick={()=>toggleActive(u)}>{u.active?'Disattiva':'Attiva'}</Button><Button variant="danger" size="sm" onClick={()=>setConfirmDel(u)}>Elimina</Button></div></Card>)}</div>)}</div><Modal open={!!techLink} onClose={()=>setTechLink(null)} title={techLink?`Link di ${techLink.name}`:''}><TextInput value={techLink?.url||''} readOnly/></Modal><ConfirmDialog open={!!confirmDel} title="Eliminare l'utente?" danger confirmLabel="Elimina definitivamente" message={confirmDel?`${confirmDel.name} verrà eliminato definitivamente.`:''} onCancel={()=>setConfirmDel(null)} onConfirm={()=>remove(confirmDel)}/></section>
}

function SensorsTab(){const[sensors,setSensors]=useState([]),[loading,setLoading]=useState(true),[syncing,setSyncing]=useState(false);useEffect(()=>{fetchAllSensors().then(r=>setSensors(r.sensors||[])).catch(()=>{}).finally(()=>setLoading(false))},[]);const toggle=async(s,id)=>{const f={hotelgio:!!s.mostra_hotelgio,chocohotel:!!s.mostra_chocohotel,brigantino:!!s.mostra_brigantino};f[id]=!f[id];setSensors(l=>l.map(x=>x.device_id===s.device_id?{...x,[`mostra_${id}`]:f[id]}:x));await updateSensorVisibility(s.device_id,f)};const sync=async()=>{setSyncing(true);try{const r=await syncSensorsFromEwelink(supabaseUrl);setSensors(r.sensors||[])}finally{setSyncing(false)}};if(loading)return <Spinner label="Carico i sensori…"/>;return <section><div className="rs-page-title"><div><h1>Sensori</h1><p>Visibilità dei sensori eWeLink per struttura</p></div><Button variant="ghost" onClick={sync}>{syncing?'Sincronizzo…':'Sincronizza'}</Button></div>{sensors.map(s=><Card key={s.device_id} className="rs-sensor"><div className="rs-sensor__info"><strong>{s.nome||s.device_id}</strong><small>{s.temperatura!=null?`${s.temperatura}°C`:'Temperatura non disponibile'}</small></div><div className="rs-hotel-toggles">{HOTELS.map(h=><button key={h.id} className={`rs-hotel-toggle ${s[`mostra_${h.id}`]?'on':''}`} onClick={()=>toggle(s,h.id)}>{h.short}</button>)}</div></Card>)}</section>}

function NavigationTab(){
 const[role,setRole]=useState(ROLES[0]),[config,setConfig]=useState(null),[status,setStatus]=useState(''),[saving,setSaving]=useState(false)
 useEffect(()=>{if(!supabase){setConfig({});return}supabase.from('app_config').select('value').eq('key',NAV_KEY).maybeSingle().then(({data})=>{try{setConfig(data?.value?JSON.parse(data.value):{})}catch{setConfig({})}}).catch(()=>setConfig({}))},[])
 const placement=(r,k)=>config?.[r]?.[k]||'off', bottomCount=r=>NAV_ITEMS.filter(([k])=>placement(r,k)==='bottom').length
 const setPlacement=(k,v)=>{setStatus('');if(v==='bottom'&&placement(role,k)!=='bottom'&&bottomCount(role)>=5){setStatus('Massimo 5 voci nella barra sotto.');return}setConfig(c=>({...c,[role]:{...(c[role]||{}),[k]:v,...(k==='planning_work'?{planning_sale:v}:{})}}))}
 const saveConfig=async()=>{if(!supabase)return;setSaving(true);setStatus('');try{const normalized=Object.fromEntries(Object.entries(config||{}).map(([r,v])=>[r,{...v,planning_sale:v?.planning_work||'off'}]));const{error}=await supabase.from('app_config').update({value:JSON.stringify(normalized)}).eq('key',NAV_KEY);if(error)throw error;setConfig(normalized);setStatus('Configurazione salvata')}catch(e){setStatus(e?.message||'Salvataggio non riuscito')}finally{setSaving(false)}}
 const perms=ROLE_PERMISSIONS[role]||[]
 return <section data-testid="settings-navigation"><div className="rs-page-title"><div><h1>Ruoli & Navigazione</h1><p>Permessi e posizione delle funzioni per ruolo</p></div></div><h2 style={{fontFamily:'Sora',fontSize:'1rem',margin:'10px 4px'}}>Ruolo da configurare</h2><Card className="rs-card--pad"><Field label="Seleziona ruolo"><select className="rs-select" value={role} onChange={e=>setRole(e.target.value)} data-testid="nav-role-select">{ROLES.map(r=><option key={r}>{r}</option>)}</select></Field><div style={{marginTop:14}}><strong style={{fontFamily:'Sora'}}>Permessi</strong><div className="rs-perm-card__list" style={{marginTop:8}}>{perms.map(p=><Badge key={p} tone="accent">{PERMISSION_LABELS[p]||p}</Badge>)}{!perms.length&&<span className="rs-field__hint">Nessun permesso</span>}</div></div></Card><h2 style={{fontFamily:'Sora',fontSize:'1rem',margin:'22px 4px 10px'}}>Navigazione</h2><Card className="rs-card--pad">{config===null?<Spinner/>:<><p className="rs-field__hint">{bottomCount(role)}/5 voci nella barra sotto · {role}</p>{NAV_ITEMS.map(([key,label])=><div className="rs-navrow" key={key}><span>{label}</span><div className="rs-navseg">{PLACEMENTS.map(([val,l])=><button key={val} className={placement(role,key)===val?'active':''} onClick={()=>setPlacement(key,val)}>{l}</button>)}</div></div>)}{status&&<p className="rs-badge rs-badge--accent" style={{display:'inline-flex',marginTop:12}}>{status}</p>}<Button variant="primary" style={{marginTop:14}} onClick={saveConfig} disabled={saving}>{saving?'Salvo…':'Salva configurazione'}</Button></>}</Card></section>
}
function AppearanceTab(){return <section><div className="rs-page-title"><div><h1>Aspetto</h1><p>Tema e dimensione interfaccia</p></div></div><Card className="rs-card--pad" style={{marginBottom:12}}><ThemeControl/></Card><Card className="rs-card--pad"><UiSizeControl/></Card></section>}
const TABS=[{id:'users',icon:'users',label:'Utenti',render:()=> <UsersTab/>},{id:'sensors',icon:'sensor',label:'Sensori',render:()=> <SensorsTab/>},{id:'navigation',icon:'sliders',label:'Ruoli',render:()=> <NavigationTab/>},{id:'usage',icon:'activity',label:'Consumi',render:()=> <UsageTab/>},{id:'appearance',icon:'sparkles',label:'Aspetto',render:()=> <AppearanceTab/>}]
function bytesToMB(b){return (b/(1024*1024)).toFixed(1)}
const DB_LIMIT_BYTES=500*1024*1024
function BarraLimite({label,valore,limite,unita=''}){const pct=Math.min(100,(valore/limite)*100),colore=pct>80?'#C81E1E':pct>50?'#B9762A':'#0F6B5C';return <div style={{marginBottom:12}}><div style={{display:'flex',justifyContent:'space-between',fontSize:12.5,marginBottom:4}}><span style={{color:'var(--rs-text-2)'}}>{label}</span><span style={{fontWeight:700,color:'var(--rs-text)'}}>{valore}{unita} / {limite}{unita}</span></div><div style={{height:8,background:'var(--rs-surface-2)',borderRadius:999,overflow:'hidden'}}><div style={{width:pct+'%',height:'100%',background:colore,borderRadius:999}}/></div></div>}
function RigaStat({label,valore}){return <div style={{display:'flex',justifyContent:'space-between',fontSize:13.5,padding:'6px 0',borderBottom:'1px solid var(--rs-line)'}}><span style={{color:'var(--rs-text-2)'}}>{label}</span><span style={{fontWeight:700,color:'var(--rs-text)'}}>{valore}</span></div>}
function UsageTab(){
  const[stats,setStats]=useState(null),[loading,setLoading]=useState(true),[error,setError]=useState('')
  const carica=async()=>{setLoading(true);setError('');try{const{data,error:err}=await supabase.rpc('get_usage_stats');if(err)throw err;setStats(data)}catch(e){setError('Errore nel caricamento: '+String(e?.message||e))}setLoading(false)}
  useEffect(()=>{carica()},[])
  return <section><div className="rs-page-title"><div><h1>Consumi</h1><p>Utilizzo del database Supabase (piano gratuito) e conteggi per struttura</p></div><Button variant="ghost" onClick={carica}>{loading?'Carico…':'↻ Aggiorna'}</Button></div>
    {error && <Card className="rs-card--pad" style={{color:'#C81E1E'}}>{error}</Card>}
    {loading ? <Spinner label="Carico i dati…"/> : stats && <>
      <Card className="rs-card--pad" style={{marginBottom:12}}>
        <strong style={{display:'block',marginBottom:12}}>Database (Apice MultiHotel)</strong>
        <BarraLimite label="Spazio occupato" valore={bytesToMB(stats.db_size_bytes)} limite={bytesToMB(DB_LIMIT_BYTES)} unita=" MB"/>
        <RigaStat label="Utenti" valore={stats.utenti}/>
        <RigaStat label="Segnalazioni" valore={stats.segnalazioni}/>
        <RigaStat label="Interventi" valore={stats.interventi}/>
        <RigaStat label="Planning lavori" valore={stats.planning_lavori}/>
        <RigaStat label="Richieste urgenti" valore={stats.richieste_urgenti}/>
        <RigaStat label="Iscrizioni push attive" valore={stats.push_subscriptions}/>
      </Card>
      {HOTELS.map(h=>{const perHotel=stats.per_hotel?.[h.id];return perHotel && <Card key={h.id} className="rs-card--pad" style={{marginBottom:12}}>
        <strong style={{display:'block',marginBottom:12}}>{h.name}</strong>
        <RigaStat label="Segnalazioni" valore={perHotel.segnalazioni}/>
        <RigaStat label="Interventi" valore={perHotel.interventi}/>
        <RigaStat label="Richieste urgenti" valore={perHotel.richieste_urgenti}/>
      </Card>})}
    </>}
  </section>
}
export default function Settings({initialTab='users',onExit}){const[tab,setTab]=useState(initialTab),active=TABS.find(t=>t.id===tab);return <div className="rs-root"><div className="rs-app"><header className="rs-settings-head"><div className="rs-settings-head__brand"><Icon name="gear"/><div><b>Impostazioni</b><small>RandApp Manutenzione</small></div></div><Button variant="ghost" size="sm" icon="logout" onClick={onExit}>Esci</Button></header><main className="rs-content">{active?.render()}</main><nav className="rs-settings-nav">{TABS.map(t=><button key={t.id} className={`rs-navbtn ${tab===t.id?'active':''}`} onClick={()=>setTab(t.id)}><Icon name={t.icon}/><small>{t.label}</small></button>)}<button className="rs-navbtn" onClick={onExit}><Icon name="home"/><small>RandApp</small></button></nav></div></div>}
