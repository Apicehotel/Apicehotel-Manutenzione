const finite01=(v,n)=>{const x=Number(v);if(!Number.isFinite(x)||x<0||x>1)throw new TypeError(`${n} must be finite 0..1`);return x}
const nonNegInt=(v,n)=>{const x=Number(v);if(!Number.isInteger(x)||x<0)throw new TypeError(`${n} must be an integer >= 0`);return x}
export function evaluateSloWindow({totalEvents,badEvents,target=0.995,maxBurnRate=1}={}){const total=nonNegInt(totalEvents,'totalEvents');const bad=nonNegInt(badEvents,'badEvents');if(bad>total)throw new TypeError('badEvents cannot exceed totalEvents');const slo=finite01(target,'target');const burnLimit=Number(maxBurnRate);if(!Number.isFinite(burnLimit)||burnLimit<0)throw new TypeError('maxBurnRate must be finite and >= 0');const allowedRate=1-slo;const observedRate=total===0?0:bad/total;const burnRate=allowedRate===0?(bad===0?0:Infinity):observedRate/allowedRate;return Object.freeze({ok:burnRate<=burnLimit,totalEvents:total,badEvents:bad,target:slo,observedBadRate:observedRate,burnRate,maxBurnRate:burnLimit})}
export function assertSloBudget(input){const result=evaluateSloWindow(input);if(!result.ok){const error=new Error('SLO_ERROR_BUDGET_EXHAUSTED');error.code='SLO_ERROR_BUDGET_EXHAUSTED';error.result=result;throw error}return result}
